syntax = "proto3";

package nosqldb.data; // Используем пакет, чтобы избежать конфликтов имен

message User {
  int32 id = 1;
  string name = 2;
  string email = 3;
  int32 age = 4;
  string city = 5;
}
